Чёткий план реконструкции я бы зафиксировал так.

Шаг 1. Сразу исправить правило same hand в hand_state.py.
compare() должен решать только по hero_cards: те же карты — обновляем тот же hand; другие карты — закрываем текущий hand и открываем новый. Все остальные отличия (player_count, hero_position, occupied_positions, геометрия) переводятся не в MATCH_NONE, а в warnings/diagnostics или максимум в weak_conflict, но без открытия нового hand по этим признакам. Это маленькая правка, но она уберёт хаотическое дробление hand.json.

Шаг 2. Ввести новый канонический слой normalizer между table_amount_logic и action_inference.
Нужен отдельный модуль, лучше новый файл, а не латка внутри старого inference: например, amounts_and_actions.py или amount_normalization.py. На его вход идут table_amount_state, street, player_count, occupied_positions, hero_position, player_states. На его выходе — final_contribution_bb_by_pos и final_contribution_street_bb_by_pos. На префлопе этот слой обязан автоматически стартовать с SB=0.5, BB=1.0, а для HU — BTN/SB=0.5, BB=1.0, после чего накладывать видимые Chips. posted_blinds здесь должны стать либо подтверждением forced-вкладов, либо диагностикой, но не единственным источником истины.

Шаг 3. Полностью заменить текущую префлоп-реконструкцию в action_inference.py.
Не “подправить”, а именно заменить её внутреннюю модель. Новый реконструктор должен идти в порядке позиций по формату стола и держать состояние: current_price_to_call, raise_level, limpers, opener_pos, three_bettor_pos, four_bettor_pos, callers_after_open, action_history. Из него должны рождаться limp, open_raise, iso_raise, call, 3bet, 4bet, cold_4bet, 5bet_jam, check. Отдельно рядом должны писаться engine_action и final_contribution_bb. Пока это не сделано, всё остальное будет продолжать ошибаться, даже если CV видит ставки идеально.

Шаг 4. Довести table_amount_logic.py до правил blind reinterpretation.
Добавить постобработку: если улица не префлоп и регион — SB или BB, он не должен уходить в posted_blinds, а должен переопределяться в обычный Chips. Если на префлопе SB/BB матчится далеко или не к своей логической blind-позиции, маркер должен либо удаляться как ложный, либо переводиться в Chips и участвовать в ставочной геометрии. При конфликте BB и Chips в одной зоне должен выживать один смысл, а не две сущности сразу.

Шаг 5. Встроить solver bridge внутрь пакета pokervision, а не держать его во внешнем launcher.
Практически это означает: вынести EngineBridge, SpotDescription и связанные хелперы из pokervision_engine_launcher_v2.py в новый pokervision/solver_bridge.py, почти без переписывания бизнес-логики. Потом вызывать solver_bridge.build_recommendation(analysis, hand) прямо из основного pipeline после hand_manager.update_or_create(...) и до build_render_state(...). Так ты перестанешь дублировать семантику между “основным проектом” и “внешним лончером”.

Шаг 6. Сделать официальный projection в PreflopContext и PostflopContext.
Из реконструкции нужно строить два стабильных payload-а:
для префлопа — node_type, hero_pos, opener_pos, three_bettor_pos, four_bettor_pos, limpers, callers, action_history;
для постфлопа — pot_before_hero, to_call, effective_stack, hero_position, villain_positions, line_context, street, player_count.
Это должен быть отдельный небольшой переводчик, а не россыпь логики по launcher-у и UI. Под эти контракты у тебя движок уже готов.

Шаг 7. Расширить models.py и render_state.py новыми нормализованными блоками.
В HandState и FrameAnalysis нужно хранить не только сырые table_amount_state и action_inference, но и нормализованные секции, например: amount_state, reconstructed_preflop, reconstructed_postflop, advisor_input, engine_result. В RenderState Нужно добавить recommended_action, recommended_amount_to, recommended_size_pct, node_type, hero_decision_debug, engine_status, analysis_panel. Сейчас модели уже умеют хранить hand/action state и render snapshot, поэтому это не ломает архитектуру, а расширяет её в нужную сторону.

Шаг 8. Обновить UI, но только после solver integration.
main.py сейчас передаёт в UI только render_state и статус; PokerTableWindow/панель должны начать отображать итоговое действие движка, ключевой node_type, action history, ranges/debug и статус расчёта. Иначе ты встроишь solver, но визуально ничего не изменится. UI здесь — не первый шаг, а последний потребитель уже нормализованного состояния.

Шаг 9. Добавить тесты не “вообще”, а именно на канонические споты ТЗ.
Здесь я бы не полагался на текущий test_core.py как на достаточное покрытие: даже если там уже есть общие pipeline-тесты, тебе нужны отдельные deterministic-тесты на реконструкцию ставок. Минимум:
UTG limp -> MP iso_raise -> CO call -> SB 3bet;
SB first-in limp -> BB check;
CO open -> BTN 3bet -> SB cold_4bet;
Постфлоп pot_before=6, increment=4 -> 66.67%;
один и тот же hand при тех же hero_cards, но шумном player_count/occupied_positions, не должен пересоздаваться. Эти тесты должны бить не по UI, а по reconstruction layer и solver projection layer.








План внедрения шага 3
3.1. Зафиксировать новый контракт preflop-реконструкции

Что сделать:
Перед правками кода зафиксировать, что для preflop есть только один канонический результат:

reconstructed_preflop
action_history_resolved
hero_context_preview
node_type_preview
opener_pos / three_bettor_pos / four_bettor_pos / limpers / callers

Именно этот resolved-результат должны читать дальше context_projection, solver bridge, render state и hand persistence. Сейчас context_projection Читает action_state["action_history"] или fallback bridge-spot, а hand_state дополнительно сливает старую и новую историю. Это и надо прекратить для preflop.

Файлы:

pokervision/action_inference.py
pokervision/hand_state.py
pokervision/context_projection.py

Критерий готовности:
У preflop есть один явный “resolved ledger”, который не равен просто сумме старых кадров.

3.2. Вынести preflop в отдельный внутренний модуль

Что сделать:
Не дописывать это дальше в середине action_inference.py, а выделить отдельный модуль, например:

pokervision/preflop_reconstruction.py

В нём сделать три уровня функций:

build_preflop_frame_observation(...)
reconstruct_preflop_from_frame(...)
reconcile_preflop_with_hand(previous_hand, current_frame_result, ...)

Сейчас весь preflop живёт монолитом внутри _infer_preflop_actions(), а postflop уже фактически использует логику “предыдущая рука + текущий street-state”. Для шага 3 нужно привести preflop к такой же дисциплине по слоям.

Почему это обязательно:
Иначе вы снова смешаете:

raw observation,
inference,
persistence,
projection для solver.
3.3. Оставить текущий single-pass только как frame-local observation

Что сделать:
Текущий цикл по actor_order оставить, но изменить его роль.

Сейчас он сразу пытается выдать финальную историю руки.
Нужно, чтобы он выдавал только:

actor_order
final_contribution_bb_by_pos
forced_blinds
street_commitments_current_frame
frame_local_actions
skipped_positions
unresolved_positions
frame_local_max_aggression

То есть это должен быть ответ на вопрос:
“Какой минимально правдоподобный порядок действий следует только из текущего финального снимка?”

Потому что в текущем коде, если после будущего 4-бета у ранней позиции уже стоит большой вклад, а у более позднего игрока в финальном снимке вклад меньше, эта поздняя позиция попадает в skipped_positions. Это логично для одного кадра, но недостаточно для истории руки.

Критерий готовности:
Frame-local слой имеет право вернуть “неполную” историю, но не имеет права притворяться полной.

3.4. Добавить hand-level reconciler для той же руки

Что сделать:
Это ключевой подшаг.

Нужен reconciler, который для той же hero_cards сравнивает:

прошлый resolved preflop ledger,
текущие final_contribution_bb_by_pos,
текущий frame-local результат.

и строит обновлённую согласованную лестницу агрессии.

Пример логики:

если ранее был UTG open 3.5,
потом появился SB 42,
потом UTG вырос до 70.5,
то новый resolved ledger должен стать не
UTG open 3.5 -> UTG open 26 -> UTG open 70.5,
а
UTG open 3.5 -> SB 3bet 42 -> UTG 4bet/5bet-type escalation.

Здесь не надо “угадывать всё по поту”; надо делать reconciliation по росту вкладов внутри той же руки. У postflop уже есть идея same_hand и перенос состояния по улице; preflop нужен такой же hand-aware слой.

Что именно хранить в reconciler state:

resolved_actions
resolved_commitments_by_pos
last_aggressor
raise_level
openers / three_bettor / four_bettor
positions_closed_to_action
last_frame_id
reconstruction_confidence
reconciliation_notes
3.5. Перестать merge-ить preflop history в hand_state.py

Что сделать:
Для preflop нельзя сохранять историю как:
existing_history + incoming_history + novel_actions.

Именно так сейчас и делается.

Нужно заменить это поведение правилом:

для Preflop — сохраняем resolved_preflop.action_history целиком как новую каноническую историю;
для Postflop — можно сохранять приращения street-state, как сейчас.

иначе:
какой бы умный reconciler вы ни написали, hand_state.py потом снова испортит результат накопительным merge.

Критерий готовности:
Внутри одной руки больше не может быть три OPEN одного и того же игрока с разными размерами, если это на самом деле одна агрессивная линия. Это ваш главный regression-check на текущем кейсе.

3.6. Пересобрать derivation node_type_preview только из resolved ledger

Что сделать:
node_type_preview, hero_context_preview, advisor_input, solver_input должны строиться только из reconciled preflop history, а не из frame-local history и не из merged-history.

сейчас node_type_preview зависит от opener_pos / three_bettor_pos / four_bettor_pos / callers_after_open / limpers, которые формируются в текущем preflop pass, а context_projection потом берёт action_state["action_history"]. Значит при плохой истории solver тоже получает плохой spot.

Критерий готовности:
Для текущей вашей руки solver/input больше не должен видеть facing_open, если resolved ledger уже содержит 3bet/4bet-эскалацию. Сейчас он именно так и ошибается.

3.7. Расширить тесты именно под runtime-эскалацию

Что сделать:
Сейчас тесты закрывают:

limp -> iso -> call -> 3bet,
SB limp -> BB check,
CO open -> BTN 3bet -> SB cold 4bet.
Но я не вижу теста на runtime-обновление одной и той же руки по кадрам и не вижу теста на 5bet_jam.

Нужно добавить минимум 6 новых тестов:

open -> 3bet -> 4bet через три кадра одной руки
open -> call -> squeeze -> opener_fold через несколько кадров
open -> 3bet -> 4bet -> 5bet_jam
SB limp -> BB raise -> SB jam для HU/short-handed
рост размера уже существующего агрессора не должен создавать новый open_raise
hand_state должен хранить resolved preflop history, а не merged frame history

Критерий готовности:
Тесты должны падать на текущем коде и зеленеть только после reconciler + storage fix.

3.8. Прогнать обязательный regression-кейс на вашей раздаче

Что сделать:
После внедрения шага 3 нужно использовать именно этот кейс hand_000041 как эталонный regression sample.

Проверки после фикса:

В action_history нет цепочки OPEN 3.5 / OPEN 26 / OPEN 70.5;
у SB учитывается 42.0, у UTG — 70.5, пот 113.5;
three_bettor_pos и/или four_bettor_pos уже не null, если линия это позволяет;
solver_input больше не facing_open, а соответствует уже эскалированному споту.

